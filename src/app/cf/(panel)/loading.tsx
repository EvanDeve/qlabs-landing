import { EsqueletoInicio } from "@/components/cf/Esqueletos";

/** Cubre el Inicio (y cualquier pantalla de /cf sin esqueleto propio). */
export default function CargandoInicio() {
  return <EsqueletoInicio />;
}
